#include "./../../lib/lib_SystemAll.dnh"
#include "./../../lib/lib_File.dnh"

function IncrementIndex(index, amount, min, max) {
    index += amount;
    if (index < min) {
        index = max;
    } else if (index > max) {
        index = min;
    }
    return index;
}